# ====================================================================
# ItaloPlanner Configuration
# ====================================================================

# Site Configuration
# ---------------------------------------------------------
# Public site URL (used for shortlinks, redirects, etc.)
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Admin Configuration
# ---------------------------------------------------------
# Editorial Admin Key - Set a secure passkey for /admin/editorial
# This is required for creating, editing, and managing stories and tips
ADMIN_KEY=your-secure-admin-key-here

# Legacy admin mode for area images (deprecated)
ITP_ADMIN=false

# Backend Configuration
# ---------------------------------------------------------
# Editorial backend: 'fs' for filesystem-based content
EDITORIAL_BACKEND=fs

# POI Backend: 'csv' | 'base44'
# - 'csv': Use local CSV file (default, no external dependencies)
# - 'base44': Use Base44 RAG-POI API (requires BASE44_API_URL and BASE44_API_KEY)
POI_BACKEND=csv

# Base44 API Configuration (only needed if POI_BACKEND=base44)
# ---------------------------------------------------------
# Base44 API endpoint URL
BASE44_API_URL=https://api.base44.internal/poi

# Base44 API key (keep this secret!)
BASE44_API_KEY=sk_your_base44_api_key_here

# Plan Shortlink Configuration
# ---------------------------------------------------------
# TTL (time-to-live) for plan shortlinks in days
PLAN_SHORTLINK_TTL_DAYS=30

# Analytics Configuration
# ---------------------------------------------------------
# Enable analytics tracking: 'on' | 'off'
NEXT_PUBLIC_ANALYTICS=off

# ====================================================================
# Example Staging Configuration (.env.staging.local)
# ====================================================================
# NEXT_PUBLIC_SITE_URL=https://staging.italoplanner.example
# EDITORIAL_BACKEND=fs
# POI_BACKEND=base44
# BASE44_API_URL=https://api.base44.internal/poi
# BASE44_API_KEY=sk_staging_*****
# PLAN_SHORTLINK_TTL_DAYS=30
# ADMIN_KEY=staging-admin-key
# NEXT_PUBLIC_ANALYTICS=off
